# cleanup.sh
A shell script that cleans up and organizes files on macOS by creating destination folders, previewing detected files, and moving them into the appropriate directories. It also supports a dry-run mode to safely test changes before applying them.
